{% extends 'base.html.twig' %}

{% block body %}
<form method="POST" action="{{ path('create_new_game') }}">
    <select name="adversaire">
        <option>Choisir un adversaire</option>
        {% for user in users %}
        <option value="{{ user.id }}" >{{ user.username }}</option>
        {% endfor %}
    </select>
    <button type="submit">Commencer la partie</button>
</form>
{% endblock %}

{% block title %}

{% endblock %}